# Учёт межиндексных связей — по подходам

*Сколько связей установил каждый подход, сколько из них новых (сверх предыдущих)
и сколько стало всего. «Ребро» — неориентированная связь между узлами двух
индексов в рамках одного отношения; «новых» = прирост к объединению предыдущих
шагов. Сгенерировано `link_accounting.py` из `crosswalk.json` / `parallels.json`
/ `reasoned_parallels.py`.*

## Подтверждённые связи (накопительно)

### ATU ↔ TMI

| # | Подход | Рёбер | Новых | Итог |
|---|---|---|---|---|
| 1 | явно в датасете (`atu_seq`, constituent) | 4573 | +4573 | 4573 |
| 2 | распарсили из меток (defining) | 75 | +66 | 4639 |
| 3 | распарсили из заметок TMI (`Type N`, AaTh-резолв) | 3491 | +1703 | 6342 |
| 4 | распарсили из summary ATU (inline-коды) | 4593 | +69 | 6411 |
| 5 | замыкание троек (inferred) | 12 | +12 | 6423 |

### Berezkin ↔ ATU

| # | Подход | Рёбер | Новых | Итог |
|---|---|---|---|---|
| 1 | распарсили из титулов (`atu_refs`, +alias-rescue, +чистка) | 584 | +584 | 584 |
| 2 | замыкание троек (через TMI-мотив) | 42 | +42 | 626 |

### Berezkin ↔ TMI

| # | Подход | Рёбер | Новых | Итог |
|---|---|---|---|---|
| 1 | распарсили из mapsofmyths (`tmi_refs` — кураторские Thompson-id из free-text поля узла; единственный *прямой* мост, +чистка) | 230 | +230 | 230 |
| 2 | замыкание троек (через defining-мотив) | 18 | +18 | 248 |

> **«Явно в датасете»** в строгом смысле — только `atu_seq` (чистая CSV-колонка
> Trilogy). Всё остальное **парсится из текста**: defining/note/summary — из
> меток и прозы ATU/TMI; `atu_refs` — из титулов Берёзкина; `tmi_refs` — из
> free-text поля узла mapsofmyths (курируется Берёзкиным/Дувакиным на стороне
> источника, но приходит текстом, который мы разбираем и чистим). «Прямой» у
> `tmi_refs` — это «единственный прямой мост Берёзкин↔TMI», а не готовый словарь.

## Отдельные вклады (входят в числа выше)

- **Старыми версиями типов ATU (alias-rescue):** **13** рёбер Berezkin↔ATU —
  цитаты на до-2004 номера, спасённые через `former_ids`/`aliases`.
- **Исправление дефектов данных:** чистка «грязных» ссылок (скобки, звёзды, `†`,
  `+`-склейки, гомоглифы) вернула висевшие в никуда рёбра —
  **+16** Berezkin↔ATU (`atu_refs`) и
  **+6** Berezkin↔TMI (`tmi_refs`),
  итого **+22**; попутно убрано ~186 битых ссылок.
- **Зеркальные ссылки:** **0** новых неориентированных рёбер — зеркало делает
  каждое ребро видимым с обоих концов (навигация), а не создаёт связи.
- **Замыкания (все три):** **72**
  рёбер (12 + 42 + 18).
- **Ещё применяли:** конкорданс **AaTh→ATU** (Wikidata) — резолвит номера AaTh в
  заметках TMI (внутри шага 3); нормализация имён/мойбейка — рёбер не даёт.

## Слои-гипотез / подсказок (НЕ подтверждённые связи, отдельными слоями на странице)

| Слой | Объём |
|---|---|
| рассуждениями (reasoned parallels, кураторские) | 17 групп / 122 пар |
| лексические, tier A (near-identical + possible) | 1874 пар |
| лексические, tier B (weaker) | 1744 пар |
| из tier A — трёхсторонние (triangles) | 23 |

Порядок надёжности на странице мотива (сверху вниз): подтверждённые прямые →
inferred (замыкание) → reasoning → near-identical → possible → weaker.

## Итоги по трём парам: было → стало

| Пара | Было (базовый подход) | Стало (все подходы) | Прирост |
|---|---|---|---|
| ATU ↔ TMI | 4573 | **6423** | +1850 |
| Berezkin ↔ ATU | 584 | **626** | +42 |
| Berezkin ↔ TMI | 230 | **248** | +18 |
| **Всего подтверждённых** | 5387 | **7297** | +1910 |

Плюс **3740** подсказок сверху (отдельными слоями).
