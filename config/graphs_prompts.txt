Locations:
Find all locations in this document and write them out in a strictly formatted json format, with "Location" objects, which include the following fields: Name, Description, Function, Adjacent to.
Do not add anything, do not explain or comment, provide only a formatted list of objects, name all objects the same and exactly as they were called before.

Time:
Extract from this document the structure of times, epochs, eras and put it in json format as a list of objects with fields Name, Description, KeyActors, KeyEvents.
Do not add anything, do not explain and do not comment, provide only a formatted list of objects, all objects when referenced repeatedly should be called the same and exactly as they were called before.

Characters:
Find all the characters in this document and write them out in a strictly formatted json format, using "Person" objects that include the following fields: Name, Description, Roles, Epithets, Attributes, and Actions.
Do not add anything, explain or comment, just provide a formatted list of objects.
When referring to objects again, use the same name as before. Ignore introductions, remarks and author's comments.

Relations:
You are provided with two documents. The first one contains a text.
Find all the relations between the characters in this document and write them out in triples: who, in what relationship (or what is being done to whom), and with who, in a strictly formatted json format as a list of objects with  the following fields: Subject, Relation, and Object fields.
Do not add, explain or comment on anything,  just provide a formatted list of objects.
When referring to objects again, use the same name as before.
The second document contains characters, their possible names and descriptions.
Make sure to identify the characters from this list in the text and call them the same each time.
Ignore introductions, remarks and author's comments.